{
  "name": "funny-rabbit-maybe-cat",
  "version": "1.0.0",
  "description": "Upload and share funny rabbit and cat media",
  "scripts": {
    "dev": "wrangler pages dev ./public",
    "deploy": "wrangler pages deploy ./public",
    "preview": "wrangler pages dev ./public --compatibility-date=2024-01-01"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
